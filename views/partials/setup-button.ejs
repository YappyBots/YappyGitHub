<form action="./<%- connected ? 'disconnect' : 'connect' %>/<%= type %>/<%= id %>" method="POST">
    <button
        class="button is-small is-<%- connected ? 'danger' : 'success' %> is-outlined is-rounded"
        type="submit"
        title="<%= connected ? 'Disconnect' : 'Connect' %> <%= type %>"
        >
        <!-- <span class="icon"> -->
            <%= connected ? '−' : '+' %>
        <!-- </span> -->
    </button>
</form>